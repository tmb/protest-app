<%= form_with(model: @rsvp_person, local: true, url: protest_rsvp_path(protest)) do |form| %>
  <% if @rsvp_person.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@rsvp_person.errors.count, "error") %> prohibited this protest from being saved:</h2>

      <ul>
      <% @rsvp_person.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_area :name %>
  </div>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_area :email %>
  </div>

  <div class="field">
    <%= form.label :phone_number %>
    <%= form.text_area :phone_number %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
